import 'package:flutter/material.dart';

/// Placeholder de la Fase 0. Contenido real: docs/02-roadmap.md, Fase 2.
class MealsPlaceholderPage extends StatelessWidget {
  const MealsPlaceholderPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Comidas')),
      body: const Center(child: Text('Fase 2 — registro diario y macros')),
    );
  }
}
