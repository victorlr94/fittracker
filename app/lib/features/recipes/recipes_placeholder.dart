import 'package:flutter/material.dart';

/// Placeholder de la Fase 0. Contenido real: docs/02-roadmap.md, Fase 3.
class RecipesPlaceholderPage extends StatelessWidget {
  const RecipesPlaceholderPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Recetas')),
      body: const Center(child: Text('Fase 3 — recetas y macros por porción')),
    );
  }
}
