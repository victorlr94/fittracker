import 'package:flutter/material.dart';

/// Placeholder de la Fase 0. Contenido real: docs/02-roadmap.md, Fase 1.
class WorkoutsPlaceholderPage extends StatelessWidget {
  const WorkoutsPlaceholderPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Entrenamientos')),
      body: const Center(child: Text('Fase 1 — catálogo, rutinas y sesiones')),
    );
  }
}
