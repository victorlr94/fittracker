/// Índice de la pestaña "Sesión" dentro del `TabBarView` de
/// `WorkoutsHomePage` — para saltar ahí desde otra pestaña (p. ej. al
/// iniciar una rutina desde la lista de rutinas). Vive en su propio
/// archivo, sin depender de `workouts_home_page.dart`, para que
/// `routines_page.dart` pueda usarlo sin crear un import circular.
const int kSessionTabIndex = 0;
